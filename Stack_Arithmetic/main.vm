// Simple VM file for push/pop and arithmetic operations

// Push constants onto the stack
push constant 10
push constant 20
push constant 30

// Add the top two values on the stack
add

// Subtract the top two values on the stack
sub

// Pop the result from the stack and store it in local[0]
pop local 0

// Push values from local[0] and add them
push local 0
push constant 5
add

// Pop the result from the stack and store it in local[1]
pop local 1

// Push and Pop in a different segment (static)
push static 5
pop static 6

// End of file
